import random

def get_numbers_ticket(min, max, quantity):
   min >= 1
   max <= 1000
   min <= quantity <= max

numbers_ticket = (range(1, 1000))
get_random_ticket = random.sample(numbers_ticket, 10)
print(get_random_ticket)